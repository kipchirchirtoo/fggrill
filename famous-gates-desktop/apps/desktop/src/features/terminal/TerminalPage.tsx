import { PageShell } from '../../components/PageShell';

export function TerminalPage() {
  return (
    <div className="flex min-h-screen flex-col bg-background">
      <PageShell title="POS Terminal" description="Point of sale — fast checkout mode" />
    </div>
  );
}
